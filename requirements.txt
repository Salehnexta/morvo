# MORVO AI BACKEND - PRODUCTION DEPENDENCIES
# Exact versions for Railway deployment compatibility
# Updated: 2025-01-07

# === CORE FRAMEWORK ===
# FastAPI and ASGI server with exact versions for stability
fastapi==0.115.9
uvicorn[standard]==0.29.0
starlette==0.45.3
pydantic==2.11.5

# === AI & AGENTS ===
# CrewAI for multi-agent orchestration
crewai==0.121.1
openai==1.75.0

# === PROTOCOLS ===
# Model Context Protocol (MCP) - REQUIRED for enhanced protocols
mcp==1.9.3
sse-starlette==2.3.6
GitPython==3.1.40
PyJWT==2.8.0

# === HTTP & NETWORKING ===
# Async HTTP client and WebSocket support
aiohttp==3.12.6
httpx==0.28.1
websockets==12.0
requests==2.32.3

# === DATABASE & CACHING ===
# Database and caching dependencies
databases==0.8.0
asyncpg==0.29.0
supabase==2.15.2
redis==5.0.1

# === SECURITY & AUTH ===
# JWT and authentication dependencies
python-jose[cryptography]==3.3.0
python-multipart==0.0.9

# === UTILITIES ===
# Environment variables and other utilities
python-dotenv==1.0.0
aiofiles==23.2.1

# === ASYNC & CONCURRENCY ===
# Core async runtime
anyio==4.7.0

# === DEVELOPMENT (Optional) ===
# Testing and debugging - can be removed for minimal deployment
pytest>=8.0.0
pytest-asyncio>=0.23.0
