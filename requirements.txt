# MORVO AI BACKEND - PRODUCTION DEPENDENCIES
# Exact versions for Railway deployment compatibility
# Updated: 2025-01-07

# === CORE FRAMEWORK ===
# FastAPI and ASGI server with exact versions for stability
fastapi==0.115.9
uvicorn[standard]==0.29.0
starlette==0.45.3
pydantic==2.11.5

# === AI & AGENTS ===
# CrewAI for multi-agent orchestration
crewai==0.121.1
openai==1.75.0

# === PROTOCOLS ===
# Model Context Protocol (MCP) - REQUIRED for enhanced protocols
mcp==1.9.3
sse-starlette==2.3.6

# === HTTP & NETWORKING ===
# Async HTTP client and WebSocket support
aiohttp==3.12.6
httpx==0.28.1
websockets==12.0
requests==2.32.3

# === DATABASE & CACHING ===
# Supabase integration and Redis caching
supabase==2.15.2
redis==5.0.1

# === SECURITY & AUTH ===
# JWT and multipart form handling
python-jose[cryptography]==3.5.0
python-multipart==0.0.20

# === UTILITIES ===
# Environment variables and Git operations
python-dotenv==1.0.1
GitPython==3.1.44
aiofiles==23.2.1

# === ASYNC & CONCURRENCY ===
# Core async runtime
anyio==4.9.0

# === DEVELOPMENT (Optional) ===
# Testing and debugging - can be removed for minimal deployment
pytest>=8.0.0
pytest-asyncio>=0.23.0
