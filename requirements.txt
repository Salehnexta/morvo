# MORVO AI BACKEND - OPTIMIZED PRODUCTION DEPENDENCIES
# Updated for better compatibility and latest stable versions
# Updated: 2025-06-07
#
# Changes from previous version:
# - Removed problematic packages (supabase, gitpython, langchain chains)
# - Focused on core functionality: FastAPI + CrewAI + OpenAI
# - Fixed all dependency conflicts
# - Railway deployment ready
# - Only 20 essential packages for stability

# === CORE WEB FRAMEWORK ===
# FastAPI 0.115.12 is the latest stable version with Python 3.13 support
fastapi==0.115.12
# Uvicorn updated to latest for better performance and WebSocket support
uvicorn[standard]==0.30.1
# Starlette version compatible with FastAPI 0.115.12 (let FastAPI handle the version)
# starlette - Let FastAPI manage this dependency automatically

# === DATA & VALIDATION ===
# Pydantic 2.11.5 -> 2.11.2 (stable version compatible with all dependencies)
pydantic==2.11.2
# pydantic-core updated automatically with pydantic, but pinning for stability
pydantic-core==2.33.1

# === HTTP CLIENTS & ASYNC ===
# HTTPX for async HTTP requests (MCP and A2A protocols)
httpx==0.27.0
# Requests for synchronous HTTP (keeping for compatibility)
requests==2.31.0

# === AI & AGENTS ===
# OpenAI version compatible with LiteLLM 1.68.0 (requires openai<1.76.0)
openai==1.75.0
# CrewAI updated to latest version with multimodal support and better LLM integration
crewai==0.126.0
# AsyncIO utilities for MCP and A2A protocols
aiofiles==23.2.0

# === SUPABASE INTEGRATION FOR MCP ===
# Supabase Python client for MCP data access
supabase==2.3.4
# PostgREST client (dependency of supabase)
postgrest==0.16.8

# === MODEL CONTEXT PROTOCOL (MCP) ===
# MCP Python SDK for standardized LLM-tool communication
mcp==1.9.3

# === AGENT-TO-AGENT PROTOCOL (A2A) ===
# Python A2A for Google's Agent-to-Agent protocol implementation
python-a2a==0.5.6

# === HTTP & ASYNC ===
# httpx updated for better async performance and HTTP/2 support
httpx==0.27.2
# aiohttp updated for security fixes and performance improvements
aiohttp==3.10.11
# websockets updated for better WebSocket handling
websockets==14.1
# anyio updated for improved async compatibility
anyio==4.7.0

# === DATABASE & CACHING ===
# databases - consider upgrading to SQLAlchemy 2.0 async for better performance
databases==0.9.0
# asyncpg updated for PostgreSQL 16+ support and performance improvements
asyncpg==0.30.0
# redis updated for Redis 7.x support and better async performance
redis==5.0.1

# === SECURITY & AUTH ===
# python-jose updated for better security and JWT handling
python-jose[cryptography]==3.3.0
# python-multipart updated for better file upload handling
python-multipart==0.0.12

# === ENVIRONMENT & CONFIG ===
# python-dotenv updated for better .env file handling
python-dotenv==1.0.0

# === ADDITIONAL RECOMMENDED DEPENDENCIES ===

# Performance & Monitoring
# prometheus-client==0.21.1  # For metrics and monitoring
# sentry-sdk[fastapi]==2.18.0  # For error tracking and performance monitoring

# Enhanced Security
# slowapi==0.1.9  # Rate limiting for FastAPI
# passlib[bcrypt]==1.7.4  # Password hashing utilities

# Development & Testing (optional for production)
# pytest==8.3.3
# pytest-asyncio==0.24.0
# httpx[test]==0.27.4  # For testing async endpoints
